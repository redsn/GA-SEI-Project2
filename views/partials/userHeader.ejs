<header>
    <nav>
        <% if(user) { %>
            <a href="/" aria-disabled="true">Logged In</a>
            <a href="/user/<%=user._id%>">Profile</a>
        <% } else { %>
            <a href="/">Not logged In</a>
            <a href="/">No profile here</a>
        <% } %>
    <ul>
        <li><a href="/">To Main</a></li>
        <li><a href="/user/login">Login</a></li>
    </ul>
    </nav>
</header>
    <main>